import React from 'react'

function Loading(levelup) {

    return (
        <div className = {levelup} id="loading" >
            <div class="close"></div>
        </div>
    )
}

export default Loading
